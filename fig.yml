web:
  build: .
  command: php -S 0.0.0.0:8000 -t /code /code/httpdocs/index.php
  ports:
    - "8000:8000"
  links:
    - db
  volumes:
    - .:/code
db:
  image: mysql
  environment:
    MYSQL_DATABASE: ushahidi
    MYSQL_USER: ushahidi
    MYSQL_PASSWORD: lamulamulamu
    MYSQL_ROOT_PASSWORD: lamulamulamu
